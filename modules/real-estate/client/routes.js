/**
 * Real Estate Module — Route Definitions
 *
 * These routes are lazy-loaded by the shell's module-loader
 * and mounted under the /real-estate base path.
 *
 * TODO: Add route components:
 *   /real-estate/listings     → ListingListPage
 *   /real-estate/listings/:id → ListingDetailPage
 *   /real-estate/properties   → PropertyListPage
 *   /real-estate/agents       → AgentDirectoryPage
 *   /real-estate/viewings     → ViewingSchedulePage
 */

const routes = [];
export default routes;
