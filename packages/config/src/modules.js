/**
 * @xapps/config — Module Registry
 *
 * Lists all available modules, default enabled/disabled state.
 * Used by tenant setup to initialize module enablement.
 *
 * Exports: MODULE_REGISTRY, getModuleConfig(name), getEnabledModules()
 */

// TODO: Implement module registry
export default {};
