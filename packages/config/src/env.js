/**
 * @xapps/config — Environment Parser
 *
 * Parses .env files using dotenv. Validates required variables with zod/joi.
 * Throws on missing required vars at startup.
 *
 * Exports: loadEnv(), getEnv(key), requireEnv(key)
 */

// TODO: Implement env loader
export default {};
