/**
 * @xapps/db — Migration Runner
 *
 * Discovers migration files from all registered modules (modules/*/server/migrations/)
 * and runs them in order. Supports up/down/status commands.
 *
 * Exports: runMigrations(), rollback(), status()
 */

// TODO: Implement migration runner
export default {};
