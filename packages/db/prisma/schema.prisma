// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PG_DATABASE_URL")
}

// TODO: Add global shared models (like Tenants, Users). 
// Module specific models should either be defined here as well or using Prisma's multi-schema feature.
