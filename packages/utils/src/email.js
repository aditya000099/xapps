/**
 * @xapps/utils — Email Sending Abstraction
 *
 * Supports SMTP (nodemailer) and API-based providers (SendGrid, Resend).
 * Exports: sendEmail(), sendTemplate()
 */

// TODO: Implement email sender
export default {};
