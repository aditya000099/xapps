/**
 * @xapps/auth — Authentication & Authorization Package
 *
 * Shared across the API server and all modules.
 * Provides a unified auth layer so no module needs to implement its own.
 *
 * Exports:
 *   - strategies: JWT, OAuth2, API Key verification
 *   - providers: Google, GitHub, email/password auth flows
 *   - tokens: generate, verify, refresh JWT tokens
 *   - permissions: define & check role-based permissions
 *   - session: session creation & management
 */

// TODO: Implement and export auth modules
export default {
  // strategies: require('./strategies'),
  // providers: require('./providers'),
  // tokens: require('./tokens'),
  // permissions: require('./permissions'),
  // session: require('./session'),
};
